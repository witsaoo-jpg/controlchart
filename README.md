# 🏥 Healthcare Control Chart System

**ระบบติดตามและวิเคราะห์อัตราการติดเชื้อในโรงพยาบาล (HA Standard)** เว็บแอปพลิเคชันสำหรับพยาบาลและทีมควบคุมการติดเชื้อ (Infection Control - IC) เพื่อใช้บันทึก วิเคราะห์ และสร้างกราฟภูมิคุ้มกัน (Control Chart) อัตโนมัติ รองรับการประเมินมาตรฐาน HA (Hospital Accreditation) และการทำ CQI (Continuous Quality Improvement)



## ✨ ฟีเจอร์เด่น (Key Features)

- 🔐 **Role-Based Login System**: ระบบล็อกอินแยกสิทธิ์การใช้งาน
  - **พยาบาลประจำหอผู้ป่วย**: บันทึก แก้ไข ลบ และดูข้อมูลเฉพาะของหอผู้ป่วยตนเอง
  - **ผู้ดูแลระบบกลาง (Admin)**: ดูภาพรวม (Overview) อัตราการติดเชื้อรวมของทั้งโรงพยาบาล และเป็นผู้กำหนดเป้าหมาย (Target)
- 📊 **Auto Control Chart**: สร้างกราฟควบคุมทางสถิติอัตโนมัติ พร้อมคำนวณเส้น Mean, UCL (Upper Control Limit) และ LCL (Lower Control Limit)
- 🧮 **Auto Calculation**: คำนวณอัตราการติดเชื้อ (Rate) ให้อัตโนมัติเมื่อกรอกจำนวน Denominator และ Numerator
- 📑 **Export to PDF & CSV**: สามารถดาวน์โหลดรายงานเป็นไฟล์ PDF พร้อมชื่อหน่วยงาน หรือดึงข้อมูลดิบเป็น CSV ได้
- 📚 **Built-in Guidelines**: มีหน้าคู่มือเกณฑ์การวินิจฉัยและสูตรคำนวณอิงตามมาตรฐานสากล (CDC / NHSN) ในตัวระบบ

## 🦠 ตัวชี้วัดที่รองรับ (Indicators)
1. **VAP** (Ventilator-Associated Pneumonia)
2. **CA-UTI** (Catheter-Associated Urinary Tract Infection)
3. **CA-BSI / CLABSI** (Central Line-Associated Bloodstream Infection)
4. **SSI** (Surgical Site Infection)

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **UI Framework**: Bootstrap 5, FontAwesome
- **Data Visualization**: Chart.js
- **Libraries**: SweetAlert2 (Popups), html2pdf.js (PDF Export)
- **Backend & Database**: Google Apps Script & Google Sheets (Serverless)

---

## 🚀 วิธีการติดตั้งและตั้งค่า (Installation & Setup)

ระบบนี้ใช้ Google Sheets เป็นฐานข้อมูลฟรี ทำให้ไม่ต้องเช่า Server เพื่อเก็บข้อมูล

### ขั้นตอนที่ 1: ตั้งค่า Google Sheets (ฐานข้อมูล)
1. สร้างไฟล์ Google Sheets ใหม่
2. เปลี่ยนชื่อแผ่นงาน (Sheet) ด้านล่างเป็น `ControlChartData`
3. ที่แถวที่ 1 ให้กำหนดหัวคอลัมน์ (Header) เรียงตามนี้ (ตัวพิมพ์เล็กทั้งหมด ห้ามเว้นวรรค):
   - A1: `id`
   - B1: `type`
   - C1: `month`
   - D1: `year`
   - E1: `denominator`
   - F1: `cases`
   - G1: `rate`
   - H1: `timestamp`
   - I1: `agency` *(สำคัญมาก: ไว้ระบุชื่อตึก)*

### ขั้นตอนที่ 2: ตั้งค่า Google Apps Script (Backend)
1. ใน Google Sheets ไปที่เมนู **ส่วนขยาย (Extensions)** > **Apps Script**
2. ลบโค้ดเดิมออกให้หมด และนำโค้ดจากไฟล์ `backend.gs` (หรือโค้ดส่วนของ doGet/doPost) ไปวาง
3. กดปุ่ม **การทำให้ใช้งานได้ (Deploy)** > **การทำให้ใช้งานได้รายการใหม่ (New deployment)**
4. เลือกประเภทเป็น **เว็บแอป (Web app)**
5. ตั้งค่าสิทธิ์การเข้าถึง (Who has access) เป็น **ทุกคน (Anyone)**
6. กด **Deploy** และก๊อปปี้ `Web App URL` ที่ได้เก็บไว้

### ขั้นตอนที่ 3: ตั้งค่า Frontend
1. เปิดไฟล์ `index.html` 
2. ค้นหาบรรทัดที่มีคำว่า `const SCRIPT_URL =` 
3. นำ `Web App URL` ที่ได้จากขั้นตอนที่ 2 มาวางแทนที่ลิงก์เดิมในเครื่องหมายคำพูด
4. ตั้งค่ารหัสผ่านเข้าใช้ระบบที่ตัวแปร `const APP_PASSWORD = 'รหัสผ่านของคุณ';`
5. Save และสามารถอัปโหลดไฟล์ `index.html` ขึ้น Web Hosting หรือ GitHub Pages เพื่อใช้งานได้ทันที!

---

## 📖 คู่มือการใช้งานเบื้องต้น (Usage Guide)

1. **การเข้าสู่ระบบ:** เลือกชื่อตึกจาก Dropdown และใส่รหัสผ่าน หากต้องการดูภาพรวมให้เลือก "ผู้ดูแลระบบกลาง (Admin)"
2. **การบันทึกข้อมูล:** ไปที่แท็บของตัวชี้วัดที่ต้องการ (เช่น VAP) > กดปุ่ม `เพิ่มข้อมูล` > เลือกเดือน/ปี และกรอกจำนวน
3. **การตั้งเป้าหมาย (Admin เท่านั้น):** กดปุ่ม `ตั้งเป้าหมาย` ระบบจะตีเส้นกราฟสีส้มเปรียบเทียบในทุกหอผู้ป่วย
4. **การดาวน์โหลดรายงาน:** ไปที่หน้า Dashboard กดปุ่ม `ดาวน์โหลด PDF` ข้อมูลกราฟและตัวเลขทั้งหมดจะถูกบันทึกเป็นรายงานที่พร้อมนำเสนอทันที

---

## 📝 เกณฑ์การคำนวณ (อิงตาม CDC / NHSN)
* **VAP, CA-UTI, CA-BSI Rate** = `(จำนวนผู้ป่วยติดเชื้อ / จำนวนวันใช้อุปกรณ์) × 1,000`
* **SSI Rate** = `(จำนวนผู้ป่วยติดเชื้อ / จำนวนผู้ป่วยที่ได้รับการผ่าตัด) × 100`

---
*Developed for Healthcare Quality Improvement (CQI) and Hospital Accreditation (HA).*
